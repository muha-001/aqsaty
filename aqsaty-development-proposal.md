# مقترح تطوير مشروع aqsaty

## الملخص التنفيذي

راجعت المستودع `muha-001/aqsaty` على GitHub، وجميع الملفات الموجودة فيه. النسخة الحالية تتكون من ملف واحد فقط هو `index.html` بحجم يقارب 1964 سطرًا. الملف يجمع HTML وCSS وJavaScript في صفحة واحدة، ويخزن المنتجات وسعر الصرف وحالة دخول المدير داخل `localStorage` في متصفح المستخدم.

المشروع الحالي مناسب كنموذج أولي لعرض منتجات التقسيط، لكنه غير جاهز لإدارة أقساط حقيقية أو بيانات زبائن حقيقية. السبب الأساسي هو عدم وجود خادم أو قاعدة بيانات أو نظام صلاحيات فعلي. لذلك فإن التطوير الصحيح ليس إضافة جدول بسيط داخل الصفحة، بل نقل المشروع إلى بنية تطبيق كاملة تفصل بين واجهة العميل، ولوحة الإدارة، وواجهة الخادم، وقاعدة البيانات.

> **النتيجة الأهم:** يمكن تنفيذ الفكرة المطلوبة بالكامل، بحيث يرتبط الزبون برقم هاتف واحد، ويمكن أن يملك عدة عقود تقسيط مستقلة، وكل عقد يرتبط بمنتج وجدول شهري خاص به وسجل دفعات مستقل.

## أولاً: تدقيق النسخة الحالية

| المجال | الموجود حاليًا | التقييم |
|---|---|---|
| واجهة العرض | صفحة عربية RTL بتصميم أزرق وذهبي وبطاقات منتجات | جيد كنموذج بصري أولي |
| المنتجات | إضافة وتعديل وحذف المنتجات مع السعر والوصف والصور | يعمل محليًا فقط |
| خيارات التقسيط | خيارات ثابتة 3 و6 و10 و12 شهرًا | موجودة للعرض، وليست عقودًا مالية |
| الحسابات | حساب قيمة القسط من السعر ÷ عدد الأشهر مع التقريب للأعلى | يحتاج إلى جدول دفعات دقيق ومعالجة فرق التقريب |
| إدارة البيانات | `localStorage` | غير مناسب لبيانات الزبائن أو التشغيل من أكثر من جهاز |
| تسجيل المدير | رمز ثابت داخل JavaScript | ثغرة أمنية خطيرة؛ الرمز ظاهر لأي شخص يفتح المصدر |
| العملاء | غير موجودين | يحتاج إلى نظام زبائن مستقل |
| عقود التقسيط | غير موجودة | يحتاج إلى نموذج عقد متعدد لكل زبون |
| الدفعات | غير موجودة | يحتاج إلى سجل دفعات وحالات تأخير |
| البحث برقم الهاتف | غير موجود | يجب إضافته عبر API آمن |
| الصور | تحفظ بصيغة Base64 داخل المتصفح | تستهلك التخزين وتضيع عند تغيير الجهاز |
| التواصل | رابط WhatsApp ورقم هاتف تجريبي `0000000000` | يجب استبدالهما بإعدادات حقيقية |
| البيانات التجريبية | تضيف منتجات تجريبية تلقائيًا عند فراغ التخزين | يجب تعطيلها في الإنتاج |
| الهوية الرسمية | تظهر عبارات تفيد الارتباط بمصرف الرافدين | يجب عدم استخدامها إلا بتفويض قانوني واضح |

## ثانياً: المميزات المطلوبة في النسخة الاحترافية

### 1. بوابة استعلام الزبون

يظهر في الصفحة الرئيسية زر واضح بعنوان **استعلام عن أقساطي**. يكتب الزبون رقم هاتفه بصيغة عراقية، ثم يحصل على صفحة ملخص تحتوي على:

- اسم الزبون بصيغة مخفية جزئيًا عند الحاجة.
- عدد الأجهزة أو المنتجات المرتبطة برقمه.
- إجمالي المبالغ الأصلية.
- إجمالي المبالغ المسددة.
- إجمالي المبالغ المتبقية.
- عدد الأشهر المسددة.
- عدد الأشهر المتبقية.
- الدفعة القادمة وتاريخ استحقاقها.
- حالة كل عقد: منتظم، مستحق، متأخر، مكتمل، أو موقوف.

استخدام رقم الهاتف وحده سهل للزبون، لكنه ليس كافيًا للحماية إذا كانت البيانات حساسة. الخيار العملي هو أن يبدأ البحث برقم الهاتف، ثم يرسل النظام رمز تحقق لمرة واحدة عبر SMS أو WhatsApp، أو يطلب رمزًا سريًا يسلّمه المكتب للزبون. يمكن تفعيل البحث بالهاتف فقط في النسخة الأولى إذا كانت طبيعة البيانات تسمح بذلك، مع إخفاء الاسم الكامل وعدم عرض أي بيانات تعريفية إضافية.

### 2. عرض عدة أجهزة للزبون نفسه

كل زبون يمكن أن يملك عددًا غير محدود من عقود التقسيط. مثال:

- العقد الأول: iPhone، بقيمة محددة، على 10 أشهر.
- العقد الثاني بعد خمسة أشهر: جهاز تبريد، بقيمة مختلفة، على 6 أشهر.
- العقد الثالث لاحقًا: جهاز آخر، بجدول مستقل.

عند إدخال الرقم، لا تظهر الأقساط كجدول واحد مختلط. تظهر بطاقة ملخص للزبون، ثم بطاقة مستقلة لكل منتج. داخل كل بطاقة يظهر جدول العقد الخاص بالمنتج فقط.

### 3. إدخال العقد من النافذة السرية

تحتفظ لوحة الإدارة بمسار محمي لإضافة العقود. يمر الإدخال بالخطوات التالية:

1. البحث عن زبون موجود برقم الهاتف، أو إنشاء زبون جديد.
2. إدخال اسم الزبون ورقم الهاتف والبيانات الاختيارية اللازمة.
3. اختيار المنتج من قائمة المنتجات، أو كتابة منتج مخصص.
4. إدخال السعر الكلي.
5. اختيار عدد الأشهر: 6 أو 10 أو 12، مع إمكانية السماح بقيم أخرى من إعدادات المدير.
6. تحديد تاريخ بداية العقد وأول تاريخ استحقاق.
7. إدخال دفعة مقدمة، إن وجدت.
8. مراجعة الجدول الذي سيولده النظام.
9. حفظ العقد وإنشاء رقم عقد واضح.

بعد الحفظ، ينشئ النظام جدولًا شهريًا كاملًا، وليس مجرد رقم للقسط. كل صف في الجدول يمثل استحقاقًا له رقم، وتاريخ، ومبلغ، وحالة، وتاريخ دفع، وملاحظة.

### 4. إضافة عقد جديد لنفس الشخص

من صفحة الزبون في لوحة الإدارة يظهر زر **إضافة عقد جديد**. ينشئ الزر عقدًا منفصلًا لا يؤثر على العقد السابق. يجب أن تكون لكل عملية تقسيط:

- رقم عقد مستقل.
- تاريخ بداية مستقل.
- عدد أشهر مستقل.
- قيمة أصلية مستقلة.
- جدول استحقاقات مستقل.
- سجل دفعات مستقل.
- حالة مستقلة.

بهذا يمكن معرفة إجمالي وضع الزبون، وفي الوقت نفسه معرفة تفاصيل كل جهاز على حدة.

### 5. تسجيل الدفعات

تحتاج لوحة الإدارة إلى شاشة **تسجيل دفعة**. يختار الموظف العقد، ثم يختار القسط المستحق أو يسجل مبلغًا جزئيًا أو دفعة مقدمة. يسجل النظام:

- المبلغ المدفوع.
- تاريخ الدفع الفعلي.
- طريقة الدفع.
- الموظف الذي سجل العملية.
- رقم إيصال أو مرجع داخلي.
- ملاحظة اختيارية.

تُحسب المبالغ من سجل الدفعات الفعلي، وليس من عدد الصفوف التي ضغط عليها الموظف. هذا يمنع الأخطاء عند دفع جزء من القسط أو دفع أكثر من قسط في عملية واحدة.

## ثالثاً: طريقة الحساب المقترحة

### القاعدة الأساسية

إذا كان السعر الكلي بعد طرح الدفعة المقدمة هو `1,200,000` دينار وعدد الأشهر `6`، فإن القسط النظري هو `200,000` دينار. عند وجود كسر، يجب أن يعالج النظام فرق التقريب في القسط الأخير بدل تجاهله.

مثال: إذا كان المتبقي `1,000,000` دينار على 6 أشهر، فإن النظام ينشئ خمسة أقساط بقيمة `166,667` دينار وقسطًا أخيرًا بقيمة `166,665` دينار، بحيث يساوي مجموع الجدول المبلغ النهائي بدقة.

### القيم التي يجب عرضها

| القيمة | طريقة احتسابها |
|---|---|
| المبلغ الأصلي | السعر الكلي للعقد |
| الدفعة المقدمة | مبلغ يسجله المدير عند إنشاء العقد |
| المبلغ المتعاقد عليه | المبلغ الأصلي ناقص الدفعة المقدمة |
| المبلغ المسدد | مجموع الدفعات المقبولة |
| المبلغ المتبقي | المبلغ المتعاقد عليه ناقص المبلغ المسدد |
| الأشهر المسددة | عدد الاستحقاقات المكتملة أو المغطاة بالكامل |
| الأشهر المتبقية | الاستحقاقات غير المكتملة |
| التأخير | الاستحقاقات التي تجاوز تاريخها ولم تسدد بالكامل |

يجب ألا يعتمد النظام على القسمة فقط لعرض الأشهر المسددة. قد يسدد الزبون قسطين في دفعة واحدة، أو يدفع جزءًا من قسط. لذلك يجب أن تكون الحالة ناتجة عن الدفعات المسجلة.

## رابعاً: نموذج البيانات المقترح

### جدول الزبائن `customers`

| الحقل | الوصف |
|---|---|
| `id` | معرف داخلي فريد |
| `full_name` | اسم الزبون |
| `phone` | رقم الهاتف بعد توحيد الصيغة |
| `phone_last4` | آخر أربعة أرقام للاستخدام في التحقق الاختياري |
| `notes` | ملاحظات داخلية لا تظهر للزبون |
| `status` | نشط أو موقوف |
| `created_at` | تاريخ الإنشاء |

### جدول المنتجات `products`

| الحقل | الوصف |
|---|---|
| `id` | معرف المنتج |
| `name` | اسم المنتج |
| `category` | فئة المنتج |
| `description` | الوصف |
| `image_url` | رابط صورة مخزنة خارج قاعدة البيانات |
| `is_active` | إظهار المنتج أو إخفاؤه |
| `created_at` | تاريخ الإضافة |

### جدول العقود `installment_contracts`

| الحقل | الوصف |
|---|---|
| `id` | معرف العقد |
| `contract_number` | رقم عقد مقروء مثل `AQ-2026-000124` |
| `customer_id` | رابط الزبون |
| `product_id` | رابط المنتج، ويمكن أن يكون فارغًا للمنتج المخصص |
| `product_name_snapshot` | اسم المنتج وقت إنشاء العقد حتى لا يتغير تاريخيًا |
| `total_amount` | السعر الكلي |
| `down_payment` | الدفعة المقدمة |
| `financed_amount` | المبلغ المقسط |
| `months` | عدد الأشهر |
| `start_date` | تاريخ بداية العقد |
| `first_due_date` | أول تاريخ استحقاق |
| `status` | نشط، متأخر، مكتمل، ملغى |
| `created_by` | الموظف الذي أنشأ العقد |

### جدول الاستحقاقات `installment_schedules`

| الحقل | الوصف |
|---|---|
| `id` | معرف الاستحقاق |
| `contract_id` | العقد المرتبط |
| `installment_number` | رقم القسط |
| `due_date` | تاريخ الاستحقاق |
| `due_amount` | المبلغ المطلوب |
| `paid_amount` | المبلغ المدفوع لهذا الاستحقاق |
| `status` | قادم، مستحق، مدفوع جزئيًا، مدفوع، متأخر |
| `paid_at` | تاريخ الإكمال |

### جدول الدفعات `payments`

| الحقل | الوصف |
|---|---|
| `id` | معرف الدفعة |
| `contract_id` | العقد المرتبط |
| `schedule_id` | الاستحقاق الذي غطته الدفعة، إذا كان محددًا |
| `amount` | مبلغ الدفعة |
| `payment_date` | تاريخ الدفع |
| `method` | نقدي، تحويل، بطاقة، أو غير ذلك |
| `receipt_number` | رقم الإيصال |
| `created_by` | الموظف الذي سجل الدفعة |
| `notes` | ملاحظات داخلية |

## خامساً: لوحة الإدارة المقترحة

ينبغي استبدال رمز الدخول الموجود داخل JavaScript بنظام مصادقة حقيقي. أقل نسخة احترافية تحتوي على حساب مدير وحسابات موظفين، مع صلاحيات مختلفة.

| الدور | الصلاحيات |
|---|---|
| المدير | إدارة الموظفين، الزبائن، المنتجات، العقود، الدفعات، التقارير، الإعدادات |
| الموظف | إضافة الزبائن والعقود وتسجيل الدفعات، دون حذف شامل أو تعديل الصلاحيات |
| المشاهد | قراءة التقارير والبيانات المسموحة فقط |

تتكون لوحة الإدارة من:

1. **لوحة مؤشرات:** إجمالي الزبائن، العقود النشطة، المبالغ المسددة، المتأخرات، الاستحقاقات القادمة.
2. **إدارة الزبائن:** بحث بالهاتف أو الاسم، صفحة تفاصيل، إضافة عقد، سجل كامل.
3. **إدارة العقود:** إنشاء وتعديل وإيقاف وطباعة العقد.
4. **جدول الدفعات:** تسجيل دفعة، تصحيح دفعة بصلاحية خاصة، وسجل تغييرات.
5. **إدارة المنتجات:** الاسم والصور والفئة والسعر والوصف وخيارات الأشهر.
6. **التقارير:** المتأخرون، الاستحقاقات خلال 7 أيام، العقود المكتملة، والمبالغ حسب الشهر.
7. **الإعدادات:** العملة، بداية الشهر، رسالة العميل، قوالب الإيصالات، وبيانات التواصل.

## سادساً: الأمان والخصوصية

هذه النقطة ضرورية لأن المشروع سيتعامل مع أرقام هواتف وبيانات مالية. يجب تنفيذ الآتي قبل تشغيل النسخة الحقيقية:

- إزالة رمز المدير من الواجهة تمامًا.
- تخزين كلمات المرور بطريقة تجزئة داخل الخادم، وعدم حفظها في JavaScript.
- استخدام جلسات آمنة وتنتهي بعد مدة مناسبة.
- حماية واجهات API من الطلبات غير المصرح بها.
- عدم إظهار الاسم الكامل أو تفاصيل حساسة في صفحة عامة عند البحث بالهاتف فقط.
- تشفير الاتصال باستخدام HTTPS.
- تسجيل كل عملية إضافة وتعديل وحذف وتسجيل دفعة في سجل تدقيق.
- منع حذف عقد له دفعات؛ يستخدم الإلغاء أو الأرشفة بدل الحذف النهائي.
- إجراء نسخ احتياطية دورية لقاعدة البيانات.
- توضيح سياسة الخصوصية وسبب جمع رقم الهاتف.
- عدم استخدام اسم أو شعار مصرف الرافدين أو وصف المنصة بأنها رسمية إلا عند توفر تفويض حقيقي.

## سابعاً: البنية التقنية المقترحة

### خيار عملي مناسب

- **الواجهة:** React أو Next.js بواجهة عربية RTL.
- **الخادم:** Node.js مع API موثق.
- **قاعدة البيانات:** PostgreSQL.
- **التخزين:** تخزين ملفات منفصل للصور والإيصالات، مع حفظ الروابط في قاعدة البيانات.
- **المصادقة:** حسابات مدير وموظفين مع أدوار وصلاحيات.
- **التقارير:** استعلامات قاعدة بيانات مع تصدير PDF أو Excel.
- **الإشعارات:** WhatsApp أو SMS لاحقًا، بعد اختيار مزود رسمي مناسب.

يمكن بدء التنفيذ على مراحل من دون خسارة المشروع الحالي. تُنقل المنتجات الحالية إلى قاعدة البيانات، ثم يُبنى نظام الزبائن والعقود، وبعد ذلك تُنقل الواجهة العامة إلى التصميم الجديد.

## ثامناً: التصميم الحصري باستخدام Canva

يمكن استخدام Canva في مرحلة الهوية البصرية والتسليمات التصميمية، وليس كبديل عن قاعدة البيانات أو لوحة الإدارة. المقترح هو إنشاء Brand Kit داخل Canva ثم إنتاج العناصر التالية:

- شعار بسيط باسم `aqsaty` أو الاسم التجاري المعتمد.
- نظام ألوان عربي حديث يوازن بين الثقة المالية والوضوح الرقمي.
- قوالب إيصالات وأرقام عقود.
- قالب بطاقة ملخص القسط.
- قالب رسالة WhatsApp للدفعة القادمة.
- أيقونات موحدة للأجهزة والعقود والدفعات.
- صور غلاف للصفحة الرئيسية وصفحات التواصل.
- دليل استخدام للهوية يحدد الألوان والخطوط والمساحات.

### اتجاه بصري مقترح

أقترح أسلوبًا مختلفًا عن الشكل الحالي الذي يعتمد بكثرة على الرموز التعبيرية. يكون التصميم أكثر هدوءًا واحترافية، مع بطاقات واضحة، أرقام كبيرة للمبالغ، حالات لونية مفهومة، ومساحات بيضاء مناسبة. يمكن الاحتفاظ باللون الأزرق كلون ثقة، وإضافة لون ذهبي أو نحاسي كلون تمييز، مع لون أخضر للمدفوع ولون برتقالي للتنبيه ولون أحمر للمتأخر.

يجب أن تكون مخرجات Canva مرجعًا بصريًا للواجهة، مثل الشعار والقوالب والأيقونات والصور التسويقية. أما ألوان Brand Kit أو مكونات القوالب، فتظل داخل بيئة Canva وفق صلاحياتها وسياساتها، ولا تُستخرج تلقائيًا لبناء نسخة خارجية دون موافقة مناسبة.

## تاسعاً: صفحات واجهة العميل

| الصفحة | الهدف |
|---|---|
| الرئيسية | تعريف الخدمة، عرض المنتجات، زر الاستعلام، وزر التواصل |
| المنتجات | البحث والتصفية حسب الفئة ومدة التقسيط |
| تفاصيل المنتج | السعر وخيارات الأشهر وزر طلب المعلومات |
| استعلام الأقساط | إدخال رقم الهاتف والتحقق وعرض الملخص |
| تفاصيل العقد | جدول شهري مستقل للمنتج مع حالة كل قسط |
| سياسة الخصوصية | شرح استخدام رقم الهاتف والبيانات |
| تواصل معنا | الهاتف وWhatsApp وساعات العمل |

### شكل صفحة الاستعلام

يبدأ الجزء العلوي ببطاقة ملخص تحتوي على أربعة أرقام رئيسية: **إجمالي العقود، إجمالي المسدد، إجمالي المتبقي، والقسط القادم**. أسفلها يظهر شريط تقدم لكل عقد. ثم تأتي بطاقات الأجهزة، وكل بطاقة تتضمن اسم المنتج، سعره، تاريخ بدايته، عدد الأشهر، المسدد، المتبقي، وحالة العقد. زر **عرض جدول الأقساط** يفتح جدولًا قابلًا للقراءة من الهاتف.

## عاشراً: خطة التنفيذ المرحلية

| المرحلة | النتيجة |
|---|---|
| المرحلة 1 | تنظيف المشروع وتقسيمه من ملف واحد إلى واجهة منظمة وإزالة البيانات التجريبية من الإنتاج |
| المرحلة 2 | إنشاء قاعدة البيانات والمصادقة ولوحة الإدارة الأساسية |
| المرحلة 3 | إضافة الزبائن والعقود متعددة المنتجات وتوليد الجداول الشهرية |
| المرحلة 4 | إضافة تسجيل الدفعات والتأخير وسجل التدقيق والتقارير |
| المرحلة 5 | بناء بوابة الاستعلام بالهاتف مع حماية مناسبة |
| المرحلة 6 | تطبيق الهوية البصرية، قوالب Canva، وتحسين تجربة الهاتف |
| المرحلة 7 | الاختبار، النسخ الاحتياطي، اختبار الصلاحيات، وتجهيز النشر |

## الحصيلة المقترحة للنسخة الأولى

أوصي بأن تكون **النسخة الأولى القابلة للاستخدام** مركزة على ست وظائف فقط: إدارة الزبائن، إضافة عدة عقود للزبون، توليد جدول 6 أو 10 أو 12 شهرًا، تسجيل الدفعات، الاستعلام برقم الهاتف، وعرض المتبقي والمسدد لكل جهاز. بعد ثبات هذه الوظائف، تُضاف الإشعارات والتقارير المتقدمة والتطبيق المحمول.

بهذا الترتيب يصبح النظام مفيدًا في العمل اليومي، ويظل قابلًا للتوسع، ولا يعرض بيانات مالية حقيقية داخل متصفح واحد أو ملف HTML مكشوف.

## توصية القرار

لا أنصح بإضافة نظام الأقساط إلى النسخة الحالية باستخدام `localStorage`. الأنسب هو اعتبار النسخة الحالية مرجعًا بصريًا أوليًا، ثم بناء نسخة ثانية بقاعدة بيانات وصلاحيات. يمكن الاحتفاظ بالتصميم العام، لكن يجب إعادة تنفيذ منطق البيانات بالكامل.

الخطوة العملية التالية هي تثبيت ثلاث قرارات قبل بدء البرمجة: الاسم التجاري النهائي، هل توجد موافقة فعلية على استخدام اسم مصرف الرافدين، وطريقة تحقق الزبون من هويته بعد إدخال رقم الهاتف. بعد ذلك يمكن البدء بتصميم مخطط قاعدة البيانات وواجهات لوحة الإدارة وصفحة استعلام الأقساط، ثم تنفيذها على المستودع.

## المراجع

[1]: https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage "MDN Web Docs: Window localStorage"

[2]: https://owasp.org/www-project-application-security-verification-standard/ "OWASP Application Security Verification Standard"

[3]: https://www.canva.dev/docs/mcp/usage-policy/ "Canva MCP Usage Policy"

[4]: https://www.postgresql.org/docs/current/ "PostgreSQL Documentation"

[5]: https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview "MDN Web Docs: HTTP overview"
